{
  "name": "mdclflash",
  "version": "1.0.0",
  "description": "Watch the user button and if event is triggert copy usb stick data to internal eMMC",
  "main": "mdclflash.js",
  "scripts": {
    "build": "tsc",
    "bundle": "tsc && node bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Markus Leitz",
  "license": "UNLICENCED",
  "devDependencies": {
  },
  "dependencies": {
    "esbuild": "^0.13.8",
    "typescript": "^4.4.4"
  }
}
